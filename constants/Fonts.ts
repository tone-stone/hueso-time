/** Display font for UI chrome (titles, labels, buttons, tags) — loaded in app/_layout.tsx. Body copy keeps the system font for readability. */
export const FontFamily = {
  display: 'SpaceMono',
} as const;

export default FontFamily;
